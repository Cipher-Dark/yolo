import 'package:flutter/material.dart';

class GinieScreen extends StatelessWidget {
  const GinieScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ginie Screen"),
    );
  }
}
